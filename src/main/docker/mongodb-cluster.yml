version: '2'
services:
  bugtrackerjhipster-mongodb:
    image: mongo:4.2.3
    ports:
      - '27017:27017'
    command: mongos --configdb bugtrackerjhipster-mongodb-config
  bugtrackerjhipster-mongodb-node:
    build:
      context: .
      dockerfile: mongodb/MongoDB.Dockerfile
    command: mongod --replSet rs1 --noprealloc --smallfiles
  bugtrackerjhipster-mongodb-config:
    image: mongo:4.2.3
    container_name: bugtrackerjhipster-mongodb-config
    command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
